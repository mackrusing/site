#!/usr/bin/env zsh

input="frontend/sass/style.scss"
output="frontend/public/resources/styles/style.css"

# feedback
echo "Watching $input -> $output"

# command
npx sass --watch --no-source-map $input $output
